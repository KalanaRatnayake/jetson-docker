name: "DEPRECATED: Workflow Dispatch (local)"
description: "This local action is deprecated. Use KalanaRatnayake/sequential-dispatcher@v1 instead."

inputs:
  _:
    description: "Deprecated"
    required: false

runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        echo "This local action has been deprecated. Use KalanaRatnayake/sequential-dispatcher@v1 instead." >&2
        exit 1
